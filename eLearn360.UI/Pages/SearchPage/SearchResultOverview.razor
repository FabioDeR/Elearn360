@page "/search/{filter}"
@using eLearn360.UI.Shared.Component


<div class="container my-5">

    <h5 class="col title">Recherche : <span class="text-muted fst-italic fw-normal">"@Filter"</span></h5>

    @if (_isLoading)
    {
        <Loader />
    }
    else
    {
@*        @if (ResultList.Count > 0)
        {
            <table class="table table-hover my-5">
                <thead>
                    <tr>
                        <th scope="col">Type</th>
                        <th scope="col">Titre</th>
                        <th scope="col">Description</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var res in ResultList)
                    {
                        <tr>
    <td class="fw-bold">@GetTraduction(res.Type)</td>
                            <td>@res.Titre</td>
                            <td>@res.Description</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {*@
            <p class="text-muted text-center my-5">Désolé ! Votre recherche n'a trouvé aucun résultat.</p>
@*        }*@

    }

</div>
